require("dotenv").config(); const mongoose = require("mongoose"); const User = require("./models/User"); const MONGO_URI = process.env.MONGO_URI || "mongodb://localhost:27017/tour-packages"; async function checkAdmin() { try { await mongoose.connect(MONGO_URI); console.log("Connected!"); const admin = await User.findOne({ email: "admin@gmail.com" }); console.log("Admin found:", admin ? "Yes" : "No"); if (admin) { console.log("Name:", admin.name); console.log("Email:", admin.email); console.log("Role:", admin.role); console.log("Password (first 20 chars):", admin.password ? admin.password.substring(0, 20) + "..." : "No password"); } } catch (err) { console.error("Error:", err); } finally { mongoose.connection.close(); } } checkAdmin();
